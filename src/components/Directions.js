import Inputs from "./Inputs";

function Directions(data) {
  const routeText = data;
  console.log(routeText);
  return (
    <div>
      <p>directions</p>
    </div>
  );
}

export default Directions;
